-- Permet de spawn l'entité depuis le spawnmenu (Sandbox)
-- Dans DarkRP, vous l’exposerez via F4 plus tard (catégorie/entité).
if CLIENT then
    language.Add("ent_swb_board", "Strategic War Board (proto)")
end
